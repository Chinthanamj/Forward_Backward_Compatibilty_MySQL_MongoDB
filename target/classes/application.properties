# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/nosql
spring.datasource.username=root
spring.datasource.password=chinthu2003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Settings (Fix for missing tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/csms_mongo
spring.data.mongodb.database=csms_mongo

# Enable Debug Logging (Optional for Troubleshooting)
logging.level.org.springframework=DEBUG
